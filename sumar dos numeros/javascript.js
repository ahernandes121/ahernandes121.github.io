
//Definicion de la suma(a, b)
function sumar(a, b) {
    return a + b;
} 

//Llamada a la funcion con los numeros 8 y 17
const numero1 = 8;
const numero2 = 17;
const numero3 = sumar (numero1,numero2);

//Mostrar el resultado en la consola del navegador
console.log(`la suma de ${numero1} y ${numero2} es ${resultado}`);